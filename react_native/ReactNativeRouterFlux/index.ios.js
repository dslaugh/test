/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import { AppRegistry } from 'react-native';
import ReactNativeRouterFlux from './app';

AppRegistry.registerComponent('ReactNativeRouterFlux', () => ReactNativeRouterFlux);
